[pytest]
testpaths = tests
addopts = -q --strict-markers
markers =
    live_aws: tests that require a real, deliberately misconfigured AWS test account.
        Never run by default: `pytest -m live_aws` opts in explicitly.
